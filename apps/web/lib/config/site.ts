import { webEnv } from "@/lib/config/env"

export const siteConfig = {
  name: "adottaungatto.it",
  title: "adottaungatto.it",
  description:
    "Annunci verificati per adottare gatti e gattini in Italia, con ricerca per luogo e filtri responsabili.",
  locale: "it_IT",
  url: webEnv.siteUrl,
}
